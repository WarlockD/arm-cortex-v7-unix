/*
 * buf.cpp
 *
 *  Created on: Apr 18, 2017
 *      Author: warlo
 */

#include "buf.h"
#include "ObjectCache.h"

namespace {
	static constexpr size_t CACHE_SIZE = 64;
	using buf_cache_t = xv6::static_cache<xv6::buf::buf_t,CACHE_SIZE>;
	static buf_cache_t buf_cache;
};

namespace xv6 {


	buf::buf(dev_t dev, daddr_t sector){
		// this is bread
	}
	~buf::buf(){
		// make sure we are synced before we exit
	}
	//uint8_t* operator() { return static_cast(uint8_t*)(_data); }

	buf::read(){
		// force a read
	}
	buf::write(){
		// force a write
	}


} /* namespace xv6 */
