/*
 * ObjectCache.cpp
 *
 *  Created on: Apr 18, 2017
 *      Author: warlo
 */

#include "ObjectCache.h"

namespace xv6 {

static volatile list_head<int> test;

template<class T, typename K, typename HASH_FUNC, size_t BUCKETS,size_t STATIC_CACHE_SIZE>

struct my_inode {
	int dev;
	int nlinks;
	int size;
	int addr[14];
};
auto hash_func = [](int k)-> int { return k; };

static_cache<my_inode,int,decltype(hash_func),16,50> inode_cache;

} /* namespace xv6 */
