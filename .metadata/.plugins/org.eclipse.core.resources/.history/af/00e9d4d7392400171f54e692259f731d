/*
 * ObjectCache.cpp
 *
 *  Created on: Apr 18, 2017
 *      Author: warlo
 */

#include "ObjectCache.h"

namespace xv6 {
class my_inode {
public:
	int dev;
	int nlinks;
	int size;
	int addr[14];
	my_inode() : dev(0), nlinks(0), size(0) {}
};
static volatile list_head<my_inode> test;

struct my_hash {
	std::size_t operator()(const int k) const { return k; }
};

typedef static_cache<my_inode,int,my_hash,50> inode_cache_t;
//template<class T, typename K, typename HASH_FUNC, size_t BUCKETS,size_t STATIC_CACHE_SIZE>
inode_cache_t inode_cache;

} /* namespace xv6 */
